{
  "name": "perk-orbit-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Perk Orbit — Voucher-First Personal Financial Assistant. Monorepo with React PWA frontend (Vercel) + FastAPI backend (separate host).",
  "scripts": {
    "install:frontend": "cd frontend && yarn install",
    "build": "cd frontend && yarn install --frozen-lockfile && yarn build",
    "dev": "cd frontend && yarn dev",
    "preview": "cd frontend && yarn preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
