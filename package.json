{
  "name": "perk-worth-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "PerkWorth — Voucher-First Personal Financial Assistant. Monorepo with React PWA frontend (Vercel) + FastAPI backend (separate host).",
  "scripts": {
    "install:frontend": "cd frontend && yarn install",
    "build": "cd frontend && yarn install --frozen-lockfile && yarn build",
    "dev": "cd frontend && yarn dev",
    "preview": "cd frontend && yarn preview"
  },
  "engines": {
    "node": ">=20.0.0 <23.0.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
